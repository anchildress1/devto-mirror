# SonarQube Cloud automatic analysis reads only this file; sonar-project.properties is ignored.
# Leave sources unset: the default scans the whole repo, including .github/ workflows.
sonar.python.version=3.12
