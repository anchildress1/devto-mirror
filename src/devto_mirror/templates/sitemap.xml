<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ home }}</loc><lastmod>{{ posts | map(attribute="modified") | max }}</lastmod></url>
{% for post in posts %}
  <url><loc>{{ home }}posts/{{ post.slug }}.html</loc><lastmod>{{ post.modified }}</lastmod></url>
{% endfor %}
{% for note in comments %}
  <url><loc>{{ home }}{{ note.path }}</loc></url>
{% endfor %}
</urlset>
